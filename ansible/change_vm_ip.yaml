- hosts: all
  gather_facts: no
  become: yes

  roles:
  - role: change_vm_ip
    vars:
      new_ip: "{{ new_ip }}"
      netmask: "{{ netmask | default('24') }}"
      gateway: "{{ gateway | default('192.168.100.1') }}"